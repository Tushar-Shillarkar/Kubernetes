apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: rs-not-in
  labels: 
    app: rs-not-in
spec: 
  selector:
    matchExpressions: 
      - {key: app , operator: NotIn , values: [rsapp,  new-app , nginx-in]  }
  replicas: 3 
  template: 
    metadata:
      name: nginx
      labels: 
        app: nginx-not-in
    spec: 
      containers:
        - name: nginx
          image: nginx:latest
          ports: 
            - containerPort: 80
